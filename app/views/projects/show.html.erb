<% provide(:title, @project.name) %>
<section id="projects-page">
  <header id="overview" class="jumbotron subhead">
    <h1><%= @project.name %></h1>
    <p class="lead"><%= link_to @project.url, @project.url, :target => "_blank" %></p>
  </header>

  <ul class="breadcrumb">
    <li>
      <%= link_to "Home", root_path %> <span class="divider">/</span>
    </li>
    <li>
      <%= link_to "Projects", projects_path %> <span class="divider">/</span>
    </li>
    <li class="active">
      <%= link_to @project.name, @project %>
    </li>
  </ul>

  <% if @project.status != "archived" %>
  <section id="project-issues">
    <header>
      <%= link_to t("issues.create"), "#new_issue", :class => "btn btn-info", :id => "create-issue" %>
      <h1>Issues</h1>
    </header>
    
    <div id="issues">
    <% @project.issues.each do |issue| %>
      <%= render issue %>
    <% end %>
    </div>

    <%= render "issues/form" %>
  </section>
  <% end %>
  
  <footer>
    <% if @project.user == current_user and @project.status != "archived" %>
    <p><i class="icon-folder-close"></i> <%= link_to t("projects.archive"), archive_project_path(@project) , :class => "archive-project" %></p>
    <% end %>
  </footer>
  
</section>