<% provide(:title, 'All users') %>
<h1>All users</h1>
<ul>
<% @users.each do |user| %>
  <li><%= link_to gravatar_for(user), user %> <%= link_to user.name, user %></li>
<% end %>
</ul>

<%= will_paginate %>